# Case 17 객체 프로그래밍 도구로서의 객체

```js
function calculateCirCleArea (radius) {
  return radius * radius = Math.PI;

function calculateRectArea(width,height) {
  return width * height;
}

class Circle {
  # radius;
}

  constructor(radius) {
    this.#radios =radius;
  }
  
  get radius () {
    return this.#radius;
  }
  
  area = () => this.#radius * this. # radius * Math . PI;
  }
  
  class Rect {
    #width;
    #height;
  
  class Rect {
  width;
  height;
  
    constructor(width,height)
     this.#width = width;
     this.#hieght=height;
    }
    
    get width() {
      return this.#width;
    }
    
    get height() {
      return this.#height;
    }
    
    area = () => this.#widht * this.#height;
    }
    
    const circle = new Circle(50);
    const rect = new Rect(150,200);
   
```
